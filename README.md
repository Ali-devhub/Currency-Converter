# Currency-Converter
A simple currency converter built using HTML, CSS, and JavaScript that converts values between different currencies. It uses the Fetch API to retrieve real-time exchange rates from an external API. JavaScript handles user input, performs calculations, and updates results dynamically. CSS ensures a clean, responsive, and user-friendly interface.
